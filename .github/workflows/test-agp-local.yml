name: Test AGP Action Local (Self-hosted)

on:
  workflow_dispatch:
    inputs:
      dry-run:
        description: 'Run in dry-run mode'
        required: true
        default: 'true'
        type: choice
        options:
          - 'true'
          - 'false'
      git-user-name:
        description: 'Git user name for commits'
        required: false
        default: 'AGP Bot'
        type: string
      git-user-email:
        description: 'Git user email for commits'
        required: false
        default: 'agp-bot@example.com'
        type: string

jobs:
  test-agp-local:
    runs-on: self-hosted

    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run AGP Action
        uses: ./.github/actions/agp-action
        with:
          working-directory: '.'
          create-pr: true
          draft-pr: true
          dry-run: ${{ inputs.dry-run }}
          verbose: true
          enable-agent: true  # Testing PR #1452 - GUIDE-1418 agent authentication fix
          validation-commands: |
            npm run build
            npm test

          # Git Configuration
          git-user-name: ${{ inputs.git-user-name }}
          git-user-email: ${{ inputs.git-user-email }}

          # Docker Registry Configuration
          docker-registry: docker-all.repo.sonatype.com
          docker-username: ${{ secrets.DOCKER_USERNAME }}
          docker-password: ${{ secrets.DOCKER_PASSWORD }}

        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}  # Testing PR #1452
          ANTHROPIC_BASE_URL: "https://api.anthropic.com"
          AGP_API_TOKEN: ${{ secrets.AGP_API_TOKEN }}
          AGP_API_URL: ${{ secrets.AGP_API_URL }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Workflow Summary
        if: always()
        run: |
          echo "✅ AGP workflow completed"
          echo "Dry run mode: ${{ inputs.dry-run }}"
          echo "Git user: ${{ inputs.git-user-name }} <${{ inputs.git-user-email }}>"
